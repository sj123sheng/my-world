作为头文件库无需构建步骤，直接 `#include <glm/glm.hpp>` 即可使用；可通过预定义宏调整行为，如 `GLM_FORCE_SWIZZLE`（启用 swizzle 操作符）、`GLM_FORCE_LEFT_HANDED`（左手坐标系）、`GLM_FORCE_DEPTH_ZERO_TO_ONE`（裁剪空间 0~1）、`GLM_FORCE_INLINE`、`GLM_FORCE_CXX17` 等。