- 每个功能以头文件声明 struct 接口、cpp 文件实现对应类的模式组织，保持头文件极简（仅 struct 定义与类声明）。
- 文件 I/O 统一通过 `std::ifstream`/`std::ofstream` 操作，路径参数统一使用 `const char*` 类型。
- 写操作优先写入 `.tmp` 临时文件再原子替换为目标路径，保证存档写入的原子性与一致性。
- MVP 简化逻辑通过注释明确标注（如 `// MVP 简化`），区分当前占位实现与发布期完整实现。