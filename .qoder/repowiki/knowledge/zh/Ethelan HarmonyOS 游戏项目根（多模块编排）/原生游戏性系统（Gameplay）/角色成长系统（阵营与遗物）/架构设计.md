该模块由两个独立的数据结构组成：`camp.h` 中的 `Camp` 结构体管理阵营等级，通过 `upgrade(sourceTraces)` 方法基于消耗源印记数进行升级；`relic.h`/`relic.cpp` 中的 `Relic` 结构体持有遗物标识、能力修改描述和倍率，并通过 `apply(Character&)` 方法将效果应用到角色上。依赖方向单向：`Relic` 向前声明 `Character` 并作用于其实例，`Camp` 自包含无外部依赖。整体采用极简的 POD 风格结构体设计，逻辑内聚于成员函数中。