- 所有子模块通过 build-profile.json5 的 modules 数组声明 srcPath 与 targets，由 Hvigor 统一装配
- 原生代码通过 CMake 构建，abiFilters 固定为 arm64-v8a 与 x86_64 双架构
- 运行时配置集中在 config/dev/*.json，schema 校验定义在 config/schema/*.schema.json
- 文档按 plans/specs 日期前缀命名，置于 docs/superpowers/ 下便于追踪迭代
- 自动化能力统一放在 automation/ 目录下，按 assets/hvigor/perf 职责划分