C++ 测试需按工程构建系统单独编译每个 `test_*.cpp`（依赖 `native/` 头文件）；Node.js 测试通过 `node tests/test_bridge_contract.mjs` 和 `node tests/test_environment_assets.mjs` 直接运行，后者会访问 `../assets/environment/manifest.json` 与 PolyHaven 远程 API。