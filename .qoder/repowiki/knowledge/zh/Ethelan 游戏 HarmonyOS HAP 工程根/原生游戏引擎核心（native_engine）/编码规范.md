- 子系统通过 `Loop` 结构体聚合而非全局单例，依赖关系在构造函数中显式声明
- 跨线程共享状态统一通过 `GameSnapshot` 快照 + `SnapshotStore` 读写分离传递
- 可变共享数据使用 `std::mutex` 或 `std::atomic` 保护，如 inputEnqueueMutex、combatEventMutex
- 时间相关计算统一使用 `Tick` 类型与 `FixedPoint` 定点数，保证确定性步进