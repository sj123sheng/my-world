- ArkTS 层只通过 N-API 调用 native 暴露的函数，不直接操作 EGL/GL 或 NativeWindow。
- native 模块按 engine/gameplay/platform 三层划分，platform/harmony 屏蔽平台差异供上层复用。
- 测试文件统一命名为 tests/test_*.cpp，每个 .cpp 对应一个独立可编译的可执行目标。
- 配置与资源通过 config/schema 的 JSON Schema 校验与 assets/manifest.json 清单统一管理。