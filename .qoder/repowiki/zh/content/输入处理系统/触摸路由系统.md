# 触摸路由系统

<cite>
**本文引用的文件**   
- [touch_router.h](file://native/engine/input/touch_router.h)
- [input_event.h](file://native/engine/input/input_event.h)
- [pointer_input.h](file://native/engine/input/pointer_input.h)
- [changed_pointer_forwarder.h](file://native/engine/input/changed_pointer_forwarder.h)
- [camera_gesture.h](file://native/engine/input/camera_gesture.h)
- [virtual_joystick.h](file://native/engine/input/virtual_joystick.h)
- [test_touch_controls.cpp](file://tests/test_touch_controls.cpp)
- [test_pointer_input.cpp](file://tests/test_pointer_input.cpp)
- [Joystick.ets](file://entry/src/main/ets/ui/Joystick.ets)
- [CombatControls.ets](file://entry/src/main/ets/ui/CombatControls.ets)
- [GamePage.ets](file://entry/src/main/ets/pages/GamePage.ets)
- [test_bridge_contract.mjs](file://tests/test_bridge_contract.mjs)
</cite>

## 更新摘要
**已进行的更改**   
- 更新了输入优先级架构部分，详细说明HitTestMode.Block和HitTestMode.Transparent的使用
- 新增Joystick组件作为纯视觉反馈层的实现细节
- 更新了触摸路由与ArkTS UI层的集成方式
- 增强了输入冲突解决策略的说明
- 添加了新的测试验证机制

## 目录
1. [简介](#简介)
2. [项目结构](#项目结构)
3. [核心组件](#核心组件)
4. [架构总览](#架构总览)
5. [详细组件分析](#详细组件分析)
6. [依赖关系分析](#依赖关系分析)
7. [性能考量](#性能考量)
8. [故障排查指南](#故障排查指南)
9. [结论](#结论)
10. [附录](#附录)

## 简介
本技术文档围绕 my-world 的触摸路由系统展开，重点解析 TouchRouter 类的事件分发机制与多点触控角色分配策略。TouchRouter 负责将屏幕上的每个指针（触摸点）自动识别为"移动"或"相机"两类角色，并在冲突时给出合理的忽略策略；同时维护活动触摸点计数、指针ID跟踪以及状态清理能力。

**最新更新**：系统已建立清晰的输入优先级架构，ArkTS UI元素（CombatControls按钮）使用HitTestMode.Block优先处理，非交互区域使用HitTestMode.Transparent透传到XComponent。Joystick组件仅作为视觉反馈层，不产生重复输入事件，确保触摸输入的唯一性和准确性。

## 项目结构
触摸路由系统位于 native/engine/input 目录下，核心头文件包括：
- touch_router.h：定义 TouchRole 枚举与 TouchRouter 类，实现事件分发与角色分配
- input_event.h：定义 InputAction 枚举与 InputEvent 结构体
- pointer_input.h：提供类型转换与平台动作映射工具函数
- changed_pointer_forwarder.h：通用指针事件转发模板
- camera_gesture.h / virtual_joystick.h：相机手势与虚拟摇杆的实现，配合 TouchRouter 使用

ArkTS UI层包含：
- Joystick.ets：纯视觉反馈摇杆组件，使用HitTestMode.Transparent透传触摸事件
- CombatControls.ets：技能按钮组，使用HitTestMode.Block拦截触摸事件
- GamePage.ets：主页面，协调各UI组件的层级关系

```mermaid
graph TB
subgraph "ArkTS UI层"
JC["Joystick<br/>HitTestMode.Transparent"] --> XComp["XComponent<br/>Native回调"]
CC["CombatControls<br/>HitTestMode.Block"] --> Bridge["Bridge<br/>pushAction"]
end
subgraph "输入子系统"
IE["InputEvent<br/>InputAction"] --> TR["TouchRouter"]
PI["pointer_input.h<br/>TryMapPointerAction/TryConvert*"] --> CF["ForwardChangedPointer"]
CF --> TR
end
subgraph "交互组件"
VJ["VirtualJoystick"] --> TR
CG["CameraGesture"] --> TR
end
subgraph "测试"
T1["test_touch_controls.cpp"]
T2["test_pointer_input.cpp"]
T3["test_bridge_contract.mjs"]
end
T1 --> TR
T2 --> PI
T3 --> JC
T3 --> CC
```

**图表来源**
- [Joystick.ets:101-172](file://entry/src/main/ets/ui/Joystick.ets#L101-L172)
- [CombatControls.ets:90-233](file://entry/src/main/ets/ui/CombatControls.ets#L90-L233)
- [GamePage.ets:245-284](file://entry/src/main/ets/pages/GamePage.ets#L245-L284)
- [touch_router.h:1-59](file://native/engine/input/touch_router.h#L1-L59)
- [test_bridge_contract.mjs:16-51](file://tests/test_bridge_contract.mjs#L16-L51)

## 核心组件
- TouchRole 枚举：定义指针的角色类别，包括 None、Movement、Camera、Ignored
- TouchRouter 类：处理 PointerDown/Move/Up/Cancel 事件，维护 roles_ 映射（pointerId -> TouchRole），提供 role() 查询、activeCount() 统计与 clear() 清理
- InputEvent 与 InputAction：描述输入事件的结构与动作类型
- ForwardChangedPointer：将平台侧的指针动作类型映射为 InputAction，并调用 sink(action, pointerId, x, y)
- VirtualJoystick 与 CameraGesture：分别用于左侧移动控制与右侧相机旋转，配合 TouchRouter 的角色分配进行工作

**更新**：新增Joystick组件作为纯视觉反馈层，CombatControls组件作为输入拦截层，形成清晰的输入优先级架构。

**章节来源**
- [touch_router.h:8-58](file://native/engine/input/touch_router.h#L8-L58)
- [input_event.h:4-24](file://native/engine/input/input_event.h#L4-L24)
- [changed_pointer_forwarder.h:5-11](file://native/engine/input/changed_pointer_forwarder.h#L5-L11)
- [virtual_joystick.h:5-61](file://native/engine/input/virtual_joystick.h#L5-L61)
- [camera_gesture.h:5-53](file://native/engine/input/camera_gesture.h#L5-L53)
- [Joystick.ets:1-175](file://entry/src/main/ets/ui/Joystick.ets#L1-L175)
- [CombatControls.ets:1-301](file://entry/src/main/ets/ui/CombatControls.ets#L1-L301)

## 架构总览
触摸路由系统的整体流程如下：
- **输入优先级**：CombatControls按钮使用HitTestMode.Block优先拦截触摸事件，Joystick使用HitTestMode.Transparent让触摸事件透传到XComponent
- **XComponent原生回调**：通过OnDispatchTouchEvent获取触摸事件，提取type、id、x、y
- **事件转发**：使用ForwardChangedPointer将type映射为InputAction，并调用sink(action, pointerId, x, y)
- **TouchRouter处理**：接收事件，校验尺寸与坐标有效性后，按动作分支处理：
  - PointerDown：若该 pointerId 未注册，则根据 x < width * 0.5f 判断左右区域，调用 assignRole 分配角色
  - PointerMove：仅当 pointerId 已存在且已分配角色时返回 true
  - PointerUp/PointerCancel：从 roles_ 中移除对应条目并返回是否成功删除
- **上层逻辑**：根据 TouchRouter::role(pointerId) 的结果驱动 VirtualJoystick 或 CameraGesture

```mermaid
sequenceDiagram
participant UI as "ArkTS UI层"
participant XComp as "XComponent"
participant Native as "原生回调"
participant Bridge as "ForwardChangedPointer"
participant Router as "TouchRouter : : handle"
participant Joystick as "VirtualJoystick"
participant Camera as "CameraGesture"
Note over UI : "CombatControls : HitTestMode.Block<br/>Joystick : HitTestMode.Transparent"
UI->>XComp : "非按钮区域触摸事件"
XComp->>Native : "OnDispatchTouchEvent"
Native->>Bridge : "type,id,x,y"
Bridge->>Bridge : "TryMapPointerAction(type)"
Bridge-->>Router : "sink(InputAction, pointerId, x, y)"
Router->>Router : "校验width/height/x/y有效性"
alt "PointerDown"
Router->>Router : "检查pointerId是否已存在"
Router->>Router : "assignRole(x < width*0.5)"
Router-->>Native : "true/false"
else "PointerMove"
Router-->>Native : "roles_.find(pointerId) != end ? true : false"
else "PointerUp/PointerCancel"
Router->>Router : "roles_.erase(pointerId)"
Router-->>Native : "删除结果"
end
Note over Joystick,Camera : "上层根据 TouchRouter : : role() 决定驱动哪个组件"
```

**图表来源**
- [Joystick.ets:105-113](file://entry/src/main/ets/ui/Joystick.ets#L105-L113)
- [CombatControls.ets:90-233](file://entry/src/main/ets/ui/CombatControls.ets#L90-L233)
- [changed_pointer_forwarder.h:5-11](file://native/engine/input/changed_pointer_forwarder.h#L5-L11)
- [pointer_input.h:27-35](file://native/engine/input/pointer_input.h#L27-L35)
- [touch_router.h:17-37](file://native/engine/input/touch_router.h#L17-L37)

## 详细组件分析

### 输入优先级架构（新增）
**更新**：系统建立了明确的输入优先级层次结构：

1. **最高优先级 - CombatControls按钮**：
   - 所有技能按钮（普攻、闪避、辉印、脉流、蚀质、终结）使用`HitTestMode.Block`
   - 调试按钮和菜单按钮同样使用`HitTestMode.Block`
   - 根节点使用`HitTestMode.None`避免自身拦截

2. **中间优先级 - Joystick视觉层**：
   - 左半屏触摸面板使用`HitTestMode.Transparent`
   - 仅负责显示摇杆视觉效果，不产生输入事件
   - 触摸事件直接透传到下层XComponent

3. **最低优先级 - XComponent原生处理**：
   - 接收透传的触摸事件
   - 通过OnDispatchTouchEvent统一处理
   - 执行原有的触摸路由逻辑

```mermaid
flowchart TD
A["用户触摸"] --> B{"是否在CombatControls区域内?"}
B --> |是| C["HitTestMode.Block<br/>拦截并处理按钮事件"]
B --> |否| D{"是否在Joystick区域内?"}
D --> |是| E["HitTestMode.Transparent<br/>显示摇杆效果"]
D --> |否| F["透传到XComponent"]
E --> F
F --> G["OnDispatchTouchEvent<br/>原生处理"]
G --> H["ForwardChangedPointer<br/>事件映射"]
H --> I["TouchRouter.handle<br/>角色分配"]
C --> J["pushAction<br/>动作处理"]
```

**图表来源**
- [CombatControls.ets:90-233](file://entry/src/main/ets/ui/CombatControls.ets#L90-L233)
- [Joystick.ets:105-113](file://entry/src/main/ets/ui/Joystick.ets#L105-L113)
- [GamePage.ets:245-284](file://entry/src/main/ets/pages/GamePage.ets#L245-L284)

### TouchRole 枚举与角色语义
- None：无角色，表示指针未被路由或未激活
- Movement：移动角色，通常绑定到左侧屏幕区域，驱动 VirtualJoystick
- Camera：相机角色，通常绑定到右侧屏幕区域，驱动 CameraGesture
- Ignored：被忽略，表示当前指针因冲突策略而被拒绝参与控制

**章节来源**
- [touch_router.h:8-13](file://native/engine/input/touch_router.h#L8-L13)

### TouchRouter 类与事件分发
- handle(event, width, height)：
  - 参数校验：width/height 必须为正且有限；event.x/event.y 必须有限
  - PointerDown：若 pointerId 已存在则直接返回 false；否则根据 x < width * 0.5f 判定左右区域，调用 assignRole 分配角色
  - PointerMove：仅当 pointerId 已在 roles_ 中存在时返回 true
  - PointerUp/PointerCancel：从 roles_ 中删除对应条目并返回是否成功
- role(pointerId)：查询指定 pointerId 的当前角色，不存在则返回 None
- activeCount()：返回 roles_ 的大小，即活动触摸点数量
- clear()：清空 roles_，重置所有状态

```mermaid
flowchart TD
Start(["进入 handle"]) --> Validate["校验 width/height/x/y 有效性"]
Validate --> Valid{"有效?"}
Valid --> |否| ReturnFalse["返回 false"]
Valid --> |是| SwitchAction{"action 类型"}
SwitchAction --> |PointerDown| CheckDup{"pointerId 已存在?"}
CheckDup --> |是| ReturnFalse
CheckDup --> |否| Assign["assignRole(x < width*0.5)"]
Assign --> SetRole["roles_[pointerId] = role"]
SetRole --> ReturnTrue["返回 true"]
SwitchAction --> |PointerMove| HasRole{"pointerId 在 roles_ 中?"}
HasRole --> |是| ReturnTrue
HasRole --> |否| ReturnFalse
SwitchAction --> |PointerUp/PointerCancel| Erase["roles_.erase(pointerId)"]
Erase --> ReturnEraseResult{"删除成功?"}
ReturnEraseResult --> |是| ReturnTrue
ReturnEraseResult --> |否| ReturnFalse
SwitchAction --> |其他| ReturnFalse
```

**图表来源**
- [touch_router.h:17-37](file://native/engine/input/touch_router.h#L17-L37)

**章节来源**
- [touch_router.h:17-46](file://native/engine/input/touch_router.h#L17-L46)

### 多点触控角色分配与冲突解决
- 左右区域自动识别：
  - 当 x < width * 0.5f 时，期望角色为 Movement（左侧）
  - 否则期望角色为 Camera（右侧）
- 冲突解决策略（assignRole）：
  - 遍历 roles_，若已存在相同期望角色，则返回 Ignored 以拒绝该指针
  - 若无冲突，返回期望角色
- 效果：
  - 同一时刻最多只有一个 Movement 和一个 Camera 角色
  - 超出限制的新指针将被标记为 Ignored，避免冲突

```mermaid
flowchart TD
A["assignRole(left)"] --> B{"left ? Movement : Camera"}
B --> C["遍历 roles_ 查找是否存在相同角色"]
C --> D{"存在?"}
D --> |是| E["返回 Ignored"]
D --> |否| F["返回期望角色"]
```

**图表来源**
- [touch_router.h:49-55](file://native/engine/input/touch_router.h#L49-L55)

**章节来源**
- [touch_router.h:49-55](file://native/engine/input/touch_router.h#L49-L55)

### Joystick组件 - 纯视觉反馈层（新增）
**更新**：Joystick组件现在仅作为视觉反馈层，不产生任何输入事件：

- **透明命中检测**：所有子元素都使用`HitTestMode.Transparent`
- **触摸监听**：仅记录触摸位置用于显示摇杆效果
- **视觉反馈**：动态显示底盘、旋钮和方向指示器
- **无输入转发**：不调用pushInput或其他输入桥接方法
- **状态管理**：追踪trackedPointer、originX/Y等状态用于视觉更新

关键特性：
- 空闲提示：左下角显示淡化的摇杆虚影
- 按下反馈：显示半透明底盘和渐变旋钮
- 移动跟随：旋钮跟随手指移动，限制在50vp半径内
- 释放清理：触摸结束时隐藏摇杆

**章节来源**
- [Joystick.ets:1-175](file://entry/src/main/ets/ui/Joystick.ets#L1-L175)

### CombatControls组件 - 输入拦截层（更新）
**更新**：CombatControls组件现在明确使用HitTestMode.Block来拦截触摸事件：

- **按钮拦截**：所有技能按钮和调试按钮都使用`HitTestMode.Block`
- **根节点跳过**：根Stack使用`HitTestMode.None`避免自身拦截
- **动作转发**：通过pushAction方法转发到原生层
- **冷却系统**：支持技能冷却环和终结技高亮显示
- **调试面板**：可折叠的调试按钮面板

**章节来源**
- [CombatControls.ets:1-301](file://entry/src/main/ets/ui/CombatControls.ets#L1-L301)

### 触摸状态管理与指针ID跟踪
- roles_ 映射：int32_t pointerId -> TouchRole，记录每个活动指针的角色
- role(pointerId)：O(1) 查找，不存在返回 None
- activeCount()：返回 roles_.size()，即活动触摸点计数
- clear()：清空 roles_，释放所有活动指针状态

**章节来源**
- [touch_router.h:39-46](file://native/engine/input/touch_router.h#L39-L46)

### 输入事件模型与桥接转发
- InputEvent：包含 action、pointerId、x、y、sequence
- InputAction：PointerDown、PointerMove、PointerUp、PointerCancel 等
- TryMapPointerAction：将平台类型映射为 InputAction
- ForwardChangedPointer：封装映射与 sink 调用，便于上层统一接入

**章节来源**
- [input_event.h:4-24](file://native/engine/input/input_event.h#L4-L24)
- [pointer_input.h:27-35](file://native/engine/input/pointer_input.h#L27-L35)
- [changed_pointer_forwarder.h:5-11](file://native/engine/input/changed_pointer_forwarder.h#L5-L11)

### 与 VirtualJoystick 和 CameraGesture 的协作
- VirtualJoystick：根据 Movement 角色的移动轨迹计算方向向量，支持死区与半径配置
- CameraGesture：根据 Camera 角色的滑动轨迹累积角度变化，支持灵敏度配置
- 上层逻辑依据 TouchRouter::role(pointerId) 的结果选择驱动哪个组件

**章节来源**
- [virtual_joystick.h:10-61](file://native/engine/input/virtual_joystick.h#L10-L61)
- [camera_gesture.h:10-53](file://native/engine/input/camera_gesture.h#L10-L53)

### 测试用例与行为验证（更新）
**更新**：新增了针对输入优先级架构的测试验证：

- test_touch_controls.cpp：
  - 验证无效尺寸与坐标导致 handle 返回 false
  - 验证 PointerDown/Move/Up/Cancel 的生命周期与角色分配
  - 验证冲突场景下新指针被标记为 Ignored
  - 验证 activeCount() 与 role() 的状态一致性
  - 验证 clear() 后所有状态复位

- test_pointer_input.cpp：
  - 验证 TryMapPointerAction 的类型映射正确性
  - 验证 TryConvertInt32/TryConvertFloat 的边界与非法值处理

- test_bridge_contract.mjs（新增）：
  - 验证Joystick组件不包含pushInput调用
  - 验证Joystick使用HitTestMode.Transparent
  - 验证所有按钮都使用HitTestMode.Block
  - 验证GamePage不直接注册触摸处理器

**章节来源**
- [test_touch_controls.cpp:17-61](file://tests/test_touch_controls.cpp#L17-L61)
- [test_pointer_input.cpp:14-33](file://tests/test_pointer_input.cpp#L14-L33)
- [test_bridge_contract.mjs:16-51](file://tests/test_bridge_contract.mjs#L16-L51)

## 依赖关系分析
TouchRouter 依赖以下组件与接口：
- InputEvent/InputAction：事件结构与动作类型
- pointer_input.h：类型转换与动作映射
- changed_pointer_forwarder.h：通用指针事件转发
- 上层逻辑：根据角色驱动 VirtualJoystick/CameraGesture

**更新**：新增ArkTS UI层依赖关系：
- Joystick组件依赖透明命中检测
- CombatControls组件依赖Block命中检测
- GamePage协调各组件层级关系

```mermaid
classDiagram
class InputEvent {
+action : InputAction
+pointerId : int32_t
+x : float
+y : float
+sequence : uint64_t
}
class InputAction {
<<enum>>
+PointerDown
+PointerMove
+PointerUp
+PointerCancel
+Attack
+Dodge
+Radiance
+Current
+Corruption
+Ultimate
}
class TouchRole {
<<enum>>
+None
+Movement
+Camera
+Ignored
}
class TouchRouter {
+handle(event, width, height) : bool
+role(pointerId) : TouchRole
+activeCount() : size_t
+clear() : void
-assignRole(left) : TouchRole
-roles_ : unordered_map<int32_t, TouchRole>
}
class ForwardChangedPointer {
+ForwardChangedPointer(type, pointerId, x, y, sink) : bool
}
class VirtualJoystick {
+begin(id, pos) : void
+move(id, pos) : void
+end(id) : void
+value() : Vec2
+clear() : void
}
class CameraGesture {
+begin(id, pos) : void
+move(id, pos) : void
+end(id) : void
+consumeDelta() : Vec2
+clear() : void
}
class Joystick {
+hitTestBehavior : Transparent
+onTouch : 仅视觉反馈
+trackedPointer : number
}
class CombatControls {
+hitTestBehavior : Block
+onClick : pushAction
+cooldownRing : 冷却显示
}
TouchRouter --> InputEvent : "消费"
TouchRouter --> InputAction : "匹配"
TouchRouter --> TouchRole : "分配"
ForwardChangedPointer --> InputAction : "映射"
VirtualJoystick --> TouchRole : "Movement"
CameraGesture --> TouchRole : "Camera"
Joystick --> XComponent : "Transparent透传"
CombatControls --> Bridge : "Block拦截"
```

**图表来源**
- [input_event.h:4-24](file://native/engine/input/input_event.h#L4-L24)
- [touch_router.h:8-58](file://native/engine/input/touch_router.h#L8-L58)
- [changed_pointer_forwarder.h:5-11](file://native/engine/input/changed_pointer_forwarder.h#L5-L11)
- [virtual_joystick.h:10-61](file://native/engine/input/virtual_joystick.h#L10-L61)
- [camera_gesture.h:10-53](file://native/engine/input/camera_gesture.h#L10-L53)
- [Joystick.ets:101-172](file://entry/src/main/ets/ui/Joystick.ets#L101-L172)
- [CombatControls.ets:90-233](file://entry/src/main/ets/ui/CombatControls.ets#L90-L233)

**章节来源**
- [input_event.h:4-24](file://native/engine/input/input_event.h#L4-L24)
- [touch_router.h:8-58](file://native/engine/input/touch_router.h#L8-L58)
- [changed_pointer_forwarder.h:5-11](file://native/engine/input/changed_pointer_forwarder.h#L5-L11)
- [virtual_joystick.h:10-61](file://native/engine/input/virtual_joystick.h#L10-L61)
- [camera_gesture.h:10-53](file://native/engine/input/camera_gesture.h#L10-L53)

## 性能考量
- 时间复杂度：
  - handle(PointerDown)：O(n) 遍历 roles_ 检测冲突，n 为当前活动指针数
  - role()/activeCount()/clear()：O(1)/O(1)/O(n)
- 空间复杂度：O(n)，存储 pointerId -> TouchRole 的映射
- **更新**：输入优先级架构优化了事件处理路径，减少了不必要的触摸事件处理

**性能优化建议**：
- 若冲突检测频繁，可引入角色计数器（movementCount/cameraCount）以降低 O(n) 遍历
- 对高频 move 事件，确保上层仅在角色存在时调用，减少无意义查找
- Joystick组件的透明命中检测避免了额外的输入处理开销

## 故障排查指南
- 症状：PointerDown 返回 false
  - 可能原因：width/height 非正或非有限；event.x/event.y 非有限；pointerId 已存在
  - 排查步骤：检查传入尺寸与坐标的有效性；确认 pointerId 唯一性

- 症状：PointerMove 始终返回 false
  - 可能原因：pointerId 未在 roles_ 中；上层未正确处理 PointerDown
  - 排查步骤：确认 PointerDown 成功分配角色；检查 role(pointerId) 返回值

- 症状：activeCount() 不降
  - 可能原因：PointerUp/PointerCancel 未触发；上层未调用 clear()
  - 排查步骤：检查事件生命周期；必要时调用 clear() 强制复位

- 症状：角色冲突导致 Ignored
  - 可能原因：同侧多点触控超过一个；期望角色已存在
  - 排查步骤：调整交互设计，避免同侧多点；或扩展 assignRole 规则

- **新增**：症状：触摸事件被错误拦截
  - 可能原因：HitTestMode设置不正确；组件层级关系混乱
  - 排查步骤：检查CombatControls的Block设置；确认Joystick的Transparent设置

**章节来源**
- [touch_router.h:17-37](file://native/engine/input/touch_router.h#L17-L37)
- [test_touch_controls.cpp:17-61](file://tests/test_touch_controls.cpp#L17-L61)

## 结论
TouchRouter 提供了简洁高效的触摸路由机制，通过左右区域自动识别与冲突解决策略，确保多点触控下的稳定角色分配。结合 VirtualJoystick 与 CameraGesture，可实现流畅的移动与相机控制体验。其状态管理清晰、接口简单，易于扩展与集成。

**最新更新**：通过建立清晰的输入优先级架构，系统实现了ArkTS UI元素与原生触摸处理的完美分离。Joystick组件作为纯视觉反馈层，CombatControls作为输入拦截层，确保了触摸输入的准确性和用户体验的一致性。

## 附录

### 触摸路由配置示例
- 基本用法：
  - 创建 TouchRouter 实例
  - 在 PointerDown 时调用 handle(event, width, height)
  - 根据 role(pointerId) 的结果驱动 VirtualJoystick 或 CameraGesture
  - 在 PointerUp/PointerCancel 时确保上层组件结束跟踪
- 示例路径参考：
  - [test_touch_controls.cpp:17-61](file://tests/test_touch_controls.cpp#L17-L61)

### 自定义角色分配规则实现指南
- 目标：修改 assignRole 的行为，例如基于更复杂的区域划分或优先级策略
- 步骤：
  - 在 TouchRouter 中重写 assignRole 方法
  - 根据业务需求调整 left 判断或增加额外条件
  - 保持冲突检测逻辑的一致性，避免破坏现有行为
- 注意事项：
  - 确保返回值为 TouchRole 的有效成员
  - 更新相关测试用例以覆盖新规则

### 输入优先级架构配置（新增）
- **CombatControls配置**：
  - 所有按钮使用`.hitTestBehavior(HitTestMode.Block)`
  - 根节点使用`.hitTestBehavior(HitTestMode.None)`
  - 确保按钮点击前拦截触摸事件

- **Joystick配置**：
  - 所有子元素使用`.hitTestBehavior(HitTestMode.Transparent)`
  - 仅监听触摸事件用于视觉更新
  - 不调用任何输入桥接方法

- **GamePage协调**：
  - 确保组件层级顺序正确
  - XComponent在最底层，Joystick在中间，CombatControls在最上层

**章节来源**
- [touch_router.h:49-55](file://native/engine/input/touch_router.h#L49-L55)
- [test_touch_controls.cpp:17-61](file://tests/test_touch_controls.cpp#L17-L61)
- [Joystick.ets:101-172](file://entry/src/main/ets/ui/Joystick.ets#L101-L172)
- [CombatControls.ets:90-233](file://entry/src/main/ets/ui/CombatControls.ets#L90-L233)
- [test_bridge_contract.mjs:16-51](file://tests/test_bridge_contract.mjs#L16-L51)