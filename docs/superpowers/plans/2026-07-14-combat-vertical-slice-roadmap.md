# 艾瑟兰战斗垂直切片实施路线

**依据规格：** `docs/superpowers/specs/2026-07-14-combat-vertical-slice-design.md`

垂直切片按依赖顺序拆为六个可独立验收的实施计划。每个阶段必须保持真机已验证的 EGL/GLES3 路径可用，并在进入下一阶段前通过自身自动化测试。

| 阶段 | 独立交付物 | 依赖 | 验收出口 |
|---|---|---|---|
| 1. 基础循环与事件 | 线程安全输入、固定时间步、类型化事件、线程安全快照 | 现有 Native 渲染循环 | macOS C++ 测试通过，现有 GLES3 真机路径不变 |
| 2. 输入与自由相机 | 双区触控、虚拟摇杆、自由第三人称相机、软锁定输入语义 | 阶段 1 | 输入/相机确定性测试和真机双指操作通过 |
| 3. 角色战斗与三源 | 四段普攻、闪避、体力、三源技能、反应和共鸣终结技 | 阶段 2 | 战斗状态与三源完整单元/集成测试通过 |
| 4. 敌人 AI | 感知/决策/执行三层 AI，三类敌人和战斗区域约束 | 阶段 3 | AI 状态转换、打断、破韧和脱困测试通过 |
| 5. 首领与关卡 | 三阶段卡洛恩、遭遇流程、战斗门、补给和快速重试 | 阶段 4 | 首领完整推进、阶段单次触发和重试复位通过 |
| 6. HUD、表现与优化 | 移动 HUD、关键 VFX/音频事件、性能降级和真机报告 | 阶段 5 | 8–12 分钟完整流程、性能与稳定性门槛通过 |

阶段 1 的详细计划见：`docs/superpowers/plans/2026-07-14-combat-foundation.md`。

后续每个阶段开始前，根据上一阶段落地后的真实接口编写对应详细计划，避免在当前代码尚未形成时锁死错误签名。阶段边界和验收出口已经固定，后续详细计划不得扩大规格范围。

## 阶段 2 验收状态（2026-07-15）

- 实现提交：`5c18eb1` 至 `d13b0e1`，覆盖双区多指输入、相机相对移动、自由相机、
  确定性软锁定、固定循环集成、桥接快照以及 OHOS Native C++17 构建修复。
- 最终审查修复：相机 target/yaw/pitch/distance 已进入 GL 与软件灰盒渲染，网格、场景物件、
  粒子和玩家共用世界到视图变换；触控仅由 ArkTS `changedTouches` 经 N-API 生产；输入
  sequence 分配与队列 push 已在线性化临界区内完成；非正相机距离配置整体回退安全默认。
- 最终复审补充：世界到视图 yaw 使用角色控制器 camera right/forward 基的严格逆变换；
  跨模块测试覆盖任意 yaw 的移动方向和软锁定前轴。玩家、粒子使用随 distance 缩放、
  不受 pitch 拉伸的屏幕 billboard，GL/软件路径在非方形视口中保持相同像素圆形。
- 自动化通过：21/21 个 C++ 测试与 1/1 个 Node 桥接契约测试退出码均为 0。
- Native 链接通过：OHOS arm64-v8a `native_game` 完整编译并链接为 aarch64 ELF 共享库。
- HAP 构建通过：Hvigor `assembleHap` 返回 `BUILD SUCCESSFUL`，生成 signed HAP 并成功安装。
- 真机验收通过：单指移动改变玩家坐标，单指相机只改变画面；双指并发使 HUD 从
  `X 0.500 Y 0.500` 变为 `X 1.000 Y 0.995` 且同步旋转画面，松手 3 秒后坐标和画面稳定。
  单指释放隔离另由 C++ 双指事件序列覆盖，阶段 2 的“真机双指操作通过”出口已完成。

## 阶段 2 真机输入链路修正（2026-07-15）

- 真机复验确认单指 UI Test 与双指 uinput 均能稳定注入，但带 `libraryname` 的 Native
  XComponent 不触发页面 `.onTouch`，旧 ArkTS 单一生产者方案因此无法驱动画面。
- 生产输入改为 Native `DispatchTouchEvent` 单一来源：每次成功取事件后只转发顶层
  changed pointer 的 `type/id/x/y`。`touchPoints/numPoints` 是活动触点快照，不作为本次
  动作列表遍历，避免第二指按下时重复生成第一指 Down 等错误事件。
- ArkTS 页面删除 `.onTouch` 与 `pushInput` 调用；N-API `pushInput` 仅保留为测试或未来外部
  输入入口，不参与当前页面生产链路，避免双重生产。
- 纯 C++ 序列测试覆盖两指活动快照下的 Down/Move/Up/Cancel，确认每次只生产一条事件、
  单指释放不清理另一指职责、未知动作不入队；最新矩阵为 C++ 21/21、Node 1/1。
- 自动化、Native 链接与 signed HAP 构建完成后，设备复验已确认 HUD 移动值与画面同步响应。

## 阶段 3 验收状态（2026-07-16）

- [x] 战斗状态与三源完整自动化出口：仓库现有 31/31 个 C++ 测试逐个编译运行通过，
  Node 桥接契约 1/1 通过，`git diff --check` 通过。
- [x] 生产构建出口：指定 SDK 的 Hvigor `assembleHap` 返回 `BUILD SUCCESSFUL`，signed HAP
  存在；构建前后 `build-profile.json5` SHA-1 一致。
- [x] 阶段 3 真机出口：设备 `2MN0224C12000754` 已完成修复版 HAP 安装、启动与九项清单
  复验。四段普攻即时显示 `连击 4`；超时后的第二击显示 `连击 1` 且按第一段造成 `8` 点
  伤害；移动和受击重置同样正确。其余八项真机证据保持通过。

阶段 3 的自动化、生产构建和真机出口全部完成，可以进入阶段 4“敌人 AI”。

## 阶段 4 验收状态（2026-07-18）

- [x] 敌人 AI 自动化出口：阶段 4 分层 AI、三类敌人、区域脱困、遭遇控制、多目标战斗、
  软锁定和桥接契约测试通过。本地纯逻辑批量为 40/40，Node 桥接契约 1/1，`git diff --check`
  通过。
- [x] 生产构建出口：指定 SDK 的 Hvigor `assembleHap` 返回 `BUILD SUCCESSFUL`，生成 signed HAP
  `entry-default-signed.hap`，SHA-256 为
  `e1f24a22a894d46d38d78fd6934d27ad777ed32db59087360884d24283315a57`。
- [x] 阶段 4 真机出口：设备 `2MN0224C12000754` 完成安装、启动、布局 dump 和截图采证。
  UI 可切换 `训练`、`兽群`、`混战`、`守卫`；HUD 分别确认 `遭遇 0`、`遭遇 2` 和
  `遭遇 3`，应用进程保持运行。

阶段 4 的自动化、生产构建和真机入口验收已完成。阶段 5 开始前应基于现有 EncounterController
扩展首领阶段、战斗门、补给和快速重试，不回退训练与阶段 4 敌人遭遇语义。

## 阶段 5 验收状态（2026-07-19）

- [x] 首领与关卡自动化出口：阶段 5 首领状态机、关卡流程、战斗门、补给、快速重试和
  桥接契约测试通过。本地纯逻辑批量为 42/42，Node 桥接契约 1/1，`git diff --check`
  通过。
- [x] 生产构建出口：指定 SDK 的 Hvigor `assembleHap` 返回 `BUILD SUCCESSFUL`，生成 signed HAP
  `entry-default-signed.hap`，SHA-256 为
  `196309d906abf9dc65bae3ec5fd162667e279d25928b1602576a5c0ed5f2d42b`。
  构建前后 `build-profile.json5` 保持空签名版本，签名内容仅临时写入用于构建，未提交。
- [x] 阶段 5 真机出口：设备 `2MN0224C12000754` 完成 signed HAP 安装、启动、布局 dump
  和截图采证。`流程`、`首领`、`推进`、`补给`、`重试` 五个新入口可见；点击 `首领` 后
  HUD 显示 `遭遇 5 · 状态 1`、`关卡 5` 和 `首领 HP 1000.0 · 韧性 300.0 · 阶段 1`；
  点击 `重试` 后 Boss 状态复位到初始 HP 和阶段，应用进程保持运行。

阶段 5 的自动化、生产构建和真机入口验收全部完成。

## 阶段 6 验收状态（2026-07-19）

- [x] HUD、表现与优化自动化出口：阶段 6 VFX 系统、性能降级、占位音频、桥接契约和
  移动 HUD 测试通过。本地纯逻辑批量为 45/45，Node 桥接契约 1/1，`git diff --check`
  通过。
- [x] 生产构建出口：指定 SDK 的 Hvigor `assembleHap` 返回 `BUILD SUCCESSFUL`，生成 signed HAP
  `entry-default-signed.hap`，SHA-256 为
  `0f8d333ff9ada0ef6e8e7499105012b5fbc600ca612d14a6382161e9207c88fb`。
  构建前后 `build-profile.json5` 保持空签名版本，签名内容仅临时写入用于构建，未提交。
- [x] 阶段 6 真机出口：设备 `2MN0224C12000754` 完成 signed HAP 安装、启动、布局 dump
  和截图采证。`调试` 按钮可见，点击后调试覆盖显示 `性能 1 · VFX 0` 等字段；点击 `首领`
  后 HUD 显示 `首领 阶段 1 · 机制 0 · 读条 0ms` 和 `遭遇 5 · 状态 1 · 性能 1 · VFX 0`；
  `PerformanceGuard` 在 FPS 40 时正确输出降级级别 1；应用进程保持运行。

阶段 6 的自动化、生产构建和真机验收全部完成。垂直切片六个阶段全部收口。
